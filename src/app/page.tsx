import { BaseForm } from '@/components/chat/base-form';

export default async function Page() {
  return (
    <div className="flex items-center justify-center h-screen">
      <BaseForm />
    </div>
  );
}
